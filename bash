# Initialize git repository
git init

# Add your files
git add replace.h replace.c  # or whatever files you have
git add .  # to add all files

# Commit your changes
git commit -m "Initial commit: Add file replacement function"
